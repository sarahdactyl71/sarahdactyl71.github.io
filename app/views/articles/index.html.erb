<h1>Blog Posts</h1>
<ul id="articles"
  <% @articles.each do |article| %>
  <li>
    <%= link_to article.title, article_path(article), class: 'article-title' %>
    <%= article.body %>
  </li>
  <% end %>
</ul>
